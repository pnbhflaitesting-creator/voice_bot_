# Core
openai>=1.51.0            # STT (transcriptions) + TTS (speech), streaming
silero-vad>=5.1.2         # Voice Activity Detection
torch>=2.2                # backend for Silero VAD
sounddevice>=0.4.6        # real-time microphone + speaker I/O (PortAudio)
numpy>=1.24
python-dotenv>=1.0
