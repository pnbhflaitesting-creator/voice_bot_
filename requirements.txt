# Core
openai>=1.51.0            # OpenAI STT/TTS + OpenAI-compatible LLM (incl. Gemini)
httpx>=0.27              # REST calls to Deepgram / ElevenLabs
silero-vad>=5.1.2         # Voice Activity Detection
torch>=2.2                # backend for Silero VAD
sounddevice>=0.4.6        # real-time microphone + speaker I/O (PortAudio)
numpy>=1.24
python-dotenv>=1.0

# Optional: noise suppression (only needed if DENOISE=spectral or HIGHPASS_HZ>0)
# noisereduce>=3.0
# scipy>=1.10
